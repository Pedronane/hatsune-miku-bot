{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Edit|Write|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "sh -c 'if command -v python3 >/dev/null 2>&1; then python3 \"$0\"; else python \"$0\"; fi' \"${CLAUDE_PROJECT_DIR}/.claude/hooks/protect_env.py\""
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Edit|Write|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "sh -c 'if command -v python3 >/dev/null 2>&1; then python3 \"$0\"; else python \"$0\"; fi' \"${CLAUDE_PROJECT_DIR}/.claude/hooks/pycompile.py\""
          }
        ]
      }
    ]
  }
}
